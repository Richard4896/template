# template
Starting template with .gitignore and .gitattributes. Since this is just template, feel free to use it, no license. After copy, licenses can be added later on.


# using in github
create new repo in github, select this template

# CLI
cd C:\git

git clone {link to template repo} new_git_project

cd new_git_project

git remote remove origin

git remote add origin {link to ur repo}

git push -u origin main
